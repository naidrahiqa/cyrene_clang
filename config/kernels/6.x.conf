# Kernel 6.x Configuration
# Use with kernel-build.sh for GKI kernels

# LTO: thin (recommended for GKI)
LTO_MODE="thin"

# Warning suppress flags (handled by kernel-build.sh)
# 6.x kernels need minimal suppression

# Recommended build command:
# bash scripts/kernel-build.sh <kernel-dir> --lto=thin

# GKI-specific flags (if needed):
# KCFLAGS="-Wno-address-of-packed-member"
