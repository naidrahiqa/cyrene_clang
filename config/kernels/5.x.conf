# Kernel 5.x Configuration
# Use with kernel-build.sh for 5.x kernels

# LTO: thin for 5.12+, off for older
# Auto-detected by kernel-build.sh

# Warning suppress flags (handled by kernel-build.sh)
# 5.0-5.11 may need 4-5 flags, 5.12+ needs minimal

# Recommended build command:
# bash scripts/kernel-build.sh <kernel-dir> --lto=auto
